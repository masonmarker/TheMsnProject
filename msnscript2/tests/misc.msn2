
class ('box', =>(

    # whats in the box?
    @ things = [1, 2, 3],

    # add something to the box
    function('add', =>(

    ), 'x'),

    # prints this box
    function('print', =>(
        
    ), 'varname'),
))

# create a box
@box1 = box()

box1.print()


