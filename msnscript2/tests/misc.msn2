
class ('box', =>(

    # whats in the box?
    @ things = [1, 2, 3],

    # add something to the box
    function('add', =>(

    ), 'x'),

    # prints this box
    function('print', =>(
        varname.print()
    ), 'varname'),
))

# create a box
@box1 = box()

# add something to the box
box1.things(=>(
    @ _things = box1.things(),
    _things.add(4),
    _things.val()
))

print(box1.things())
