# simplifying with functions
~ collatz (number) -> result
  -- env(True)
	-- while (?number? != 1, if (?number? % 2 == 0, @number/=2,=>(@number*=3,@number+=1)))
  -- @result = ?number?


print(collatz(100))