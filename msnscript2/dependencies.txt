

--- GENERAL DEPENDENCIES
--- these are all the dependencies used
    by the msn2 interpreter, though not all programs
    may use all of these dependencies, as they are
    imported when an msn2 program requires it.
    this is as of 2.0.387

pip install bs4
pip install html5lib
pip install openai
pip install requests
pip install flask
pip install flask_restful
pip install flask_cors
pip install -U pywinauto
pip install openpyxl
pip install pyperclip
pip install requests-html
pip install tiktoken

-- LANG DEPENDENCIES
JavaScript: node.js
C / C++: gcc  (easily installed with 'choco install mingw')
              (this switches Python alias to 'py', and you
               may have to reinstall dependencies)