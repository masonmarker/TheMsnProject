
# fresh console
if (windows(), console('cls'), console('clear'))
 
# import 
import ('lib/http.msn2')
import ('lib/user.msn2')

# 
process('process5000', =>(
    ENDPOINT(?localhost?, ?http_default_flask_port?, '/stuff', {})
))

process('process5001', =>(
    ENDPOINT(?localhost?, 5001, '/stuff', {})
))


private(shell('usershell'))